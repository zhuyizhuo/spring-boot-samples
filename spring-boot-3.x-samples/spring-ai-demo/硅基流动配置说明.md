# 硅基流动 AI 配置使用说明

## 🎉 配置文件已更新完成

您的 `application.yml` 文件已成功更新，现在包含了硅基流动等8种AI服务的配置选项。

## 🚀 硅基流动配置步骤

### 1. 获取API密钥

1. 登录 **硅基流动官网**: https://siliconflow.cn
2. 进入 **控制台** → **API密钥**
3. 点击 **生成新密钥**
4. 复制您的API密钥（格式类似: `sk-xxxxxxxx`）

### 2. 更新配置文件

在 `src/main/resources/application.yml` 文件中，找到硅基流动配置部分：

```yaml
# 硅基流动配置 - 推荐配置
spring:
  config:
    activate:
      on-profile: siliconflow
  ai:
    openai:
      api-key: YOUR_SILICONFLOW_API_KEY_HERE  ← 将这里替换为您的实际密钥
      base-url: https://api.siliconflow.cn/v1
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
          max-tokens: 2048
```

**请将 `YOUR_SILICONFLOW_API_KEY_HERE` 替换为您的真实API密钥。**

### 3. 启动应用

**使用Maven启动**:
```bash
cd e:\git\github\spring-boot-samples\spring-boot-3.x-samples\spring-ai-demo
mvn spring-boot:run -Dspring-boot.run.profiles.active=siliconflow
```

**使用JAR包启动**:
```bash
cd e:\git\github\spring-boot-samples\spring-boot-3.x-samples\spring-ai-demo
mvn package -DskipTests
java -jar target/spring-ai-demo.jar --spring.profiles.active=siliconflow
```

### 4. 测试验证

应用启动后（端口8083），通过以下方式测试：

**Web界面测试**:
- 访问: http://localhost:8083
- 进行聊天对话测试

**API测试命令**:
```bash
# 基础聊天测试
curl -X POST http://localhost:8083/ai/chat \
  -H "Content-Type: application/json" \
  -d '{
    "content": "你好，请介绍一下你自己",
    "conversationId": "test-001",
    "style": "friendly"
  }'

# 代码生成测试
curl -X POST http://localhost:8083/ai/code \
  -H "Content-Type: application/json" \
  -d '{
    "action": "generate",
    "language": "Java",
    "description": "实现一个简单的计算器类",
    "complexity": "medium"
  }'

# 代码审查测试
curl -X POST http://localhost:8083/ai/code \
  -H "Content-Type: application/json" \
  -d '{
    "action": "review",
    "language": "JavaScript",
    "code": "function calculate(a, b) { return a + b * 2; }",
    "focus": "performance"
  }'
```

## 🎯 推荐模型选择

在硅基流动中，您可以尝试以下模型：

### 对话类模型
- `deepseek-chat` - DeepSeek对话模型（推荐）
- `qwen-turbo` - 阿里通义千问（速度快）
- `chatglm-turbo` - 智谱GLM（中文友好）
- `minimax-latest` - 月之暗面最新模型

### 代码类模型
- `deepseek-coder` - 专门代码生成
- `codellama` - Meta代码模型

### 修改模型示例

如果想换模型，修改配置文件：

```yaml
spring:
  config:
    activate:
      on-profile: siliconflow
  ai:
    openai:
      api-key: 您的密钥
      base-url: https://api.siliconflow.cn/v1
      chat:
        options:
          model: qwen-turbo        # 改这里换成其他模型
          temperature: 0.7
          max-tokens: 2048
```

## 🔧 参数调优建议

### Temperature (创造性)
- `0.1-0.3`: 适合编程、事实性问题
- `0.5-0.7`: 适合日常对话（推荐）
- `0.8-1.0`: 适合创意写作

### Max Tokens (响应长度)
- `512-1000`: 简短回答
- `2048`: 中等长度（推荐）
- `4096`: 长文本生成

### 针对不同用途的配置

**日常对话**:
```yaml
options:
  model: deepseek-chat
  temperature: 0.7
  max-tokens: 2048
```

**编程辅助**:
```yaml
options:
  model: deepseek-chat
  temperature: 0.3
  max-tokens: 4096
```

**创意写作**:
```yaml
options:
  model: deepseek-chat
  temperature: 0.8
  max-tokens: 2048
```

## 🐛 常见问题解决

### Q1: 提示"API Key无效"
**解决步骤**:
1. 检查密钥是否正确复制（不要有多余空格）
2. 确认在硅基流动控制台已生成密钥
3. 等待几分钟让密钥生效
4. 重新启动应用

### Q2: 请求超时
**解决方案**:
1. 检查网络连接
2. 尝试其他模型（如qwen-turbo）
3. 减少max-tokens参数

### Q3: 中文回复质量不好
**优化方法**:
1. 使用专门的模型：`qwen-turbo`
2. 降低temperature到0.3-0.5
3. 在提示词中明确要求"请用中文回答"

## 📊 硅基流动优势

- ✅ **新用户福利**: 14元免费额度（约2000万Token）
- ✅ **速度快**: 国内服务器，访问稳定
- ✅ **模型丰富**: 20+主流模型可选
- ✅ **中文优化**: 对中文对话支持好
- ✅ **价格透明**: 清晰的计费标准

## 🔄 其他AI服务配置

如果您想尝试其他AI服务，只需修改 `--spring.profiles.active` 参数：

```bash
# DeepSeek
--spring.profiles.active=deepseek

# 通义千问
--spring.profiles.active=qwen

# GLM智谱
--spring.profiles.active=glm

# 本地Ollama
--spring.profiles.active=ollama
```

## 📞 技术支持

- **硅基流动官网**: https://siliconflow.cn
- **API文档**: https://docs.siliconflow.cn/
- **客服支持**: 通过官网客服联系

---

**🎉 配置完成后，您就可以开始体验强大的AI功能了！**

如果遇到任何问题，请检查：
1. API密钥是否正确
2. 网络连接是否正常  
3. 应用是否在8083端口正常启动
4. 日志中是否有错误信息